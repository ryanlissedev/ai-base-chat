# Test configuration for guest access

# Core authentication (required)
AUTH_SECRET=test-secret-key-for-development-only

# Guest access configuration
# Set to 'all' to allow all models, or specify comma-separated list
ANONYMOUS_MODELS=all
ANONYMOUS_CREDITS=10000
ANONYMOUS_RPM=10000
ANONYMOUS_RPMONTH=10000

# AI Gateway (for model access) - mocked in tests
AI_GATEWAY_API_KEY=test-gateway-key
AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1/ai

# Provider keys (mocked in tests - not used due to API interception)
OPENAI_API_KEY=sk-test-mock-key-for-testing
ANTHROPIC_API_KEY=sk-ant-test-mock-key-for-testing

# Test mode flag for enabling mocks
NODE_ENV=test
PLAYWRIGHT_TEST=true

# Database (isolated test database)
DATABASE_URL=file:./test.db
POSTGRES_URL=postgresql://test_user:test_password@localhost:5432/test_db

# Langfuse (test configuration)
LANGFUSE_SECRET_KEY=test-langfuse-secret
LANGFUSE_PUBLIC_KEY=test-langfuse-public
LANGFUSE_BASEURL=https://cloud.langfuse.com

# Optional providers
GOOGLE_GENERATIVEAI_API_KEY=test
XAI_API_KEY=test