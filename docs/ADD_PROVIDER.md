# How to add a new provider
1. Add the icon from lobehub to `components/get-provider-icon.tsx`
2. Download the icon as svg (mono) from https://icons.lobehub.com/ and place it in `public/providers`
3. Add the filename to `app/(models)/get-provider-icon-url.tsx`